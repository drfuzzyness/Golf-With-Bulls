using UnityEngine;
using System.Collections;

public class MouseLook : MonoBehaviour {

	public Vector2 mouseSensitivity;
	public Vector2 maxRotation;

	void Update() {

//		float rotateX = Input.GetAxis("Mouse Y") * -mouseSensitivity.y;
//		float rotateY = Input.GetAxis("Mouse X") * mouseSensitivity.x;

//		transform.rotation.eulerAngles = new Vector3( rotateX, rotateY, 0 );

	}
}
